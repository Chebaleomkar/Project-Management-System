# Database Configuration for local development with Docker
# Copy this file to .env and update values as needed

# Database settings - use these when running Django locally (outside Docker)
DB_NAME=project_management
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5433

# Note: If you're running Django inside Docker, change DB_HOST to 'db' and DB_PORT to '5432'
